# 安全说明

## 不要公开提交

请不要提交以下内容：

- AI API Key、飞书 Webhook、Cookie、浏览器登录资料或访问令牌。
- `%APPDATA%\ContentOpsAgentV2` 下的状态、凭据、备份、企业素材和生成图片。
- 企业账号、客户信息、内部链接或真实发布数据。
- 未脱敏的浏览器缓存、历史记录、截图和日志。

## 报告安全问题

不要把未修复的 Key、Cookie、SSRF、任意文件读写、未授权远程控制或跨账号数据泄露细节直接发到公开 Issue。请先由项目维护者建立私下渠道，再提供最小复现、受影响版本和不含秘密的日志片段。

本地服务默认只绑定 `127.0.0.1`，没有设计为公网服务。若要放到服务器或增加远程控制，必须先补充身份认证、权限隔离、审计和威胁模型，不能直接暴露 17851 端口。
